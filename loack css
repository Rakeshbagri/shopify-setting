<div class="main_lock">
<div class="container">
  <div class="row">    
    <div class="lock-pad col-12">
      {% if section.settings.title != blank %}
      	<h3>{{ section.settings.title }}</h3>
      {% endif %}
      {% if section.settings.sub-title != blank %}
      	<p class="text">{{ section.settings.sub-title }}</p>
      {% endif %}
        {% if section.settings.button_label != blank %}
          <a href="{{ section.settings.button_link }}?product-id={{ section.settings.product_id }}&price-id={{ section.settings.price_id }}&plan-name={{ section.settings.plan_name_url }}" class="btn_lock"> 
  {{ section.settings.button_label }}  
          </a>
        {% endif %}
    </div>    
  </div>
</div>
</div>







/* new lock */

.main_lock { background:#f5f5f5; margin-top:-20px; padding:25px 0; margin-bottom:40px; }

.lock-pad { display:block; border:4px solid #000000; background:#faf7f0; padding:0px;   text-align:center;   }

.lock-pad h3 { color:#fff; font-size:36px;    font-family: 'core_sans_g55_medium'; letter-spacing:2px; background:#000; padding:15px; display:block; }

.lock-pad .text { margin:0px; padding:15px 0 0 0; font-size:22px; }
.lock-pad .text b{  font-family: 'core_sans_g85_heavy'; }
.lock-pad .btn_lock { background: #FC8329 0% 0% no-repeat padding-box;
  box-shadow: 0px 3px 6px #000000CC;
  border:4px solid #FFFFFF;
  border-radius: 18px; font-size:36px; padding:15px 50px;  color:#fff; position:relative; bottom:-30px;   font-family: 'core_sans_g65_bold';
}

.btn-20:hover,
.lock-pad .btn_lock:hover { color:#fff;
  background-color: #1c5459; }


/* */


/* col-refer */

.col-refer { background:#287880;  padding:15px 0; text-align:center;  }
.col-refer h6 { color:#fff; font-size:32px; text-transform:none; display:inline-block; vertical-align:middle; margin:0 20px 0 0; }

.col-refer h6 span { font-size:46px; line-height:1;  vertical-align:middle;  text-shadow: 2px 2px 2px rgba(0, 0, 0, 0.88); font-family: 'core_sans_g85_heavy'; }
.col-refer .btn-20 span { font-size:30px; line-height:1;  font-family: 'core_sans_g85_heavy'; vertical-align:middle; }

.col-refer .btn-20 { box-shadow: 0px 1px 2px #000000; }
 


/* col-refer */
